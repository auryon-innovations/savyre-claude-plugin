---
description: Show whether Savyre Chat enforcement is idle or active. Use when the user runs /savyre-status.
---

# /savyre-status (Claude Code)

```bash
node "$HOME/Documents/Projects/savyre-claude-plugin/scripts/savyre-cli.mjs" status
```

Windows PowerShell:

```powershell
node "$env:USERPROFILE/Documents/Projects/savyre-claude-plugin/scripts/savyre-cli.mjs" status
```

Speak `userMessage` only. Report idle vs enforced in plain language.
